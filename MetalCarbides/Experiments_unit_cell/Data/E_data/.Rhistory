datos <-read.table("data.for.MAPE.dat", header=TRUE, sep= "")
attach(datos)
names(datos)
datos <-read.table("data.for.MAPE.dat", header=TRUE, sep= "")
attach(datos)
names(datos)
library(MLmetrics)
MAPE(y_pred = pred, y_true = dft)
MAPE(y_pred = pred, y_true = dft)
#calculating Root-Mean-Square-Error (RMSE)
RMSE(dft,pred)
MAE(y_pred = pred, y_true = dft)
MAPE(y_pred = pred, y_true = dft)
R2(dft,pred)
R2(y_pred=pred, y_true=dft)
save.image("~/Desktop/carbides/genesis/mo2c/training/R/R.Rproj.RData")
load("~/Desktop/carbides/genesis/ti2c/R/R.Rproj.RData")
names(datos)
attach(datos)
names(datos)
mae(y_pred=pred, y_true=dft)
MAE(y_pred=pred, y_true=dft)
library(MLmetrics)
MAE(y_pred=pred, y_true=dft)
datos <-read.table("data.for.MAPE.dat", header=TRUE, sep= "")
attach(datos)
names(datos)
MAE(y_pred=pred, y_true=dft)
RMSE(y_pred=pred, y_true=dft)
